css: mise en page, style et les animations

syntax:
    propriete:valeur;
    color:blue;
    font-size:12px;

difficultes: - variete (un nombre enorme de propriete)
             - selection (selectors)
=> inlince css (<balise style="code css"></balise>)
(affecter le style directement a la balise)
=> moyenant les selecteurs 
(dans un fichier css, dans la balise style(head))